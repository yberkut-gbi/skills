# Coaching note — EN-5603 / PR #20
_2026-06-10 · fe-ship interactive_

## What shipped
Reworked `skills/shared-memory/fe-setup/SKILL.md` step 2 to scaffold per-agent/IDE/OS MCP configs. Single-file change (7 insertions, 2 deletions).

## What went well
- Scope was tightly defined and the dependency (Story 1 / EN-5600) was already merged, so the matrix in `MCP-SETUP.md` was ready to reference.
- The "reference, don't duplicate" constraint from the ticket description directly shaped the implementation: no paths are repeated in `SKILL.md`, everything defers to `MCP-SETUP.md` § "Config per agent / IDE". That's the right pattern for maintainability.
- All 6 acceptance criteria were mapped 1-to-1 to changes in the file; nothing was missed or over-built.
- Committable vs. user-global distinction is clearly encoded in the prose (write to repo vs. emit instructions).

## What to watch
- The `fe-setup` skill is now tightly coupled to the section heading in `MCP-SETUP.md` ("Config per agent / IDE"). If that heading is renamed, the cross-reference in step 2 silently breaks. Consider adding a note in `MCP-SETUP.md` that this heading is referenced by `fe-setup`.
- Step 2's MCP config bullet tells the agent to "detect the OS" but doesn't specify *how* (e.g. `uname`, `$OSTYPE`, env heuristics). This is intentional for a prompt — the executing agent figures it out — but if agents are inconsistent, a short note on detection heuristics could be added to `MCP-SETUP.md` later.

## Cost record
null — run interactively, not via the headless runner.

