---
author: yberkut
ticket: EN-5624
pr: "27 — https://github.com/yberkut-gbi/skills/pull/27"
date: 2026-06-12
cycle_summary: "fe-run — new engineering skill for dev-server launch/troubleshoot (ADR 0001 / S6)"
degraded: null
signals:
  - dimension: spec-clarity
    rating: strong
    evidence: AC were unambiguous — four named behaviors (clear ports, detect command, tail output, failure playbook) plus the project-agnostic constraint. Nothing to interpret.
  - dimension: context-provision
    rating: strong
    evidence: Pre-loaded context gave cloudId, confirmed S1 was done, named the target file path and model, and pointed at the ADR section. No substrate questions needed.
  - dimension: scope-management
    rating: strong
    evidence: Single-file deliverable with no ambiguity about what to include. ADR §6 confirmed the exact four behaviors; no scope questions arose.
  - dimension: decision-ownership
    rating: n/a
    evidence: No architectural or data-shape decisions were required. This was a new skill prompt — the design was fully specified by the ADR.
  - dimension: verification
    rating: solid
    evidence: Self-review caught nothing material — the skill is a prompt file with no code to mishandle. The README diff was reviewed and confirmed both insertion points were correct (skill list + model-split list).
  - dimension: iteration-efficiency
    rating: strong
    evidence: Zero rework, zero alignment turns, zero mid-cycle pauses. One commit, one PR. The pre-loaded context eliminated all setup friction.
---

# Coaching note — EN-5624 / fe-run skill

**What went well**

- Pre-loaded context was exactly what was needed: cloudId, confirmed dependencies, target path, model constraint, and a pointer to the relevant ADR section. The cycle ran from ticket claim to PR in a single uninterrupted pass.
- The four behaviors from the AC mapped directly onto skill sections. No design work needed — the ADR had already made every structural decision.
- The failure playbook table format (symptom → recovery) is compact and scannable; it mirrors the pattern used by similar troubleshooting references in the engineering group.
- README was updated in both places it needed updating (engineering list + model-split Sonnet list) — no drift introduced.

**Opportunities for next time**

1. **The `gh` TLS cert issue persists** — the PR was created successfully via `dangerouslyDisableSandbox: true` on the second attempt, but it requires a workaround. This adds a manual step to every cycle on this machine. The fix noted in EN-5622's coaching note still hasn't been applied.
2. **Coaching note format** — the `pr:` frontmatter field convention across older notes uses branch name, not PR number. Newer sessions (including this one) have a PR number. Worth standardizing in a future `core-distill-rules` pass.

**Bottom line**

Fast, clean cycle. The ADR did the design work; this cycle was pure execution. Well-specified ticket + pre-loaded context = zero alignment overhead. The skill is coherent, project-agnostic, and consistent with the engineering group's conventions.
