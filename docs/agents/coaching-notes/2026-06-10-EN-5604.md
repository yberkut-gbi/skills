---
author: yberkut
ticket: EN-5604
pr: https://github.com/yberkut-gbi/skills/pull/21
date: 2026-06-10
cycle_summary: Updated fe-check-setup to detect agent/IDE/OS and name the precise MCP config file path, add substrate detection, and offer re-verify — all referencing MCP-SETUP.md as the single source of truth.
signals:
  - dimension: spec-clarity
    rating: strong
    evidence: Acceptance criteria were explicit and checkable (four bullet ACs). The context note "use MCP-SETUP.md as the source of truth, don't duplicate paths inline" resolved the one non-obvious design decision before coding started — no clarifying rounds needed.
  - dimension: context-provision
    rating: strong
    evidence: Pointing to Stories 1–4 as merged and naming MCP-SETUP.md § "Config per agent / IDE" as the canonical reference gave the agent exactly the right anchoring. No re-reading of earlier work was necessary.
  - dimension: scope-management
    rating: strong
    evidence: One file, additive changes only. The boundary "don't duplicate the matrix" prevented scope from drifting toward re-embedding the path table inline, keeping the change minimal and the matrix maintainable in one place.
  - dimension: iteration-efficiency
    rating: growth_area
    evidence: Terminal heredoc / special-character handling caused ~3 wasted turns when writing the PR body (same pattern as EN-5602). The workaround (Python temp file) was reached quickly, but the issue recurs. A standing rule to always use --body-file with a pre-written file for PR bodies would eliminate this class of friction entirely.
---

# Coaching note — EN-5604: fe-check-setup names exact MCP config file per agent/IDE/OS

**What went well**

- **Spec was airtight.** Four explicit, binary ACs and the critical constraint ("reference MCP-SETUP.md, don't duplicate") made the implementation decision-free. The agent went straight from reading the ticket to writing the change — zero back-and-forth.
- **Context pointer was precise.** "Stories 1–4 merged, MCP-SETUP.md is the canonical source" was the right level of context — enough to anchor the work without re-litigating what was already done.
- **Scope discipline held.** The "don't duplicate the matrix" constraint was respected throughout: the skill references `MCP-SETUP.md § "Config per agent / IDE"` rather than copying rows into its own text. This keeps the matrix single-sourced and future-proof.

**Opportunities for next time**

1. **PR body via shell — still the same trap.** Heredoc / special-character quoting in the shell caused a few wasted turns writing the PR body, identical to the EN-5602 cycle. The fix (`--body-file` + Python-written temp file) is known. The rule should be standing: **always use `--body-file` for PR bodies that contain markdown formatting or special characters**. This is a workflow habit, not a spec or context gap — one more repetition of the same friction is enough evidence to make it a team rule.

**Bottom line**

Fast, clean cycle. Tight spec, right-sized context, no scope drift. The one friction point is mechanical and well-understood — the same shell quoting hazard that appeared in EN-5602. Two occurrences is a pattern; this should graduate from a coaching note observation to a standing rule in `team-rules.md` so `fe-distill-rules` can pick it up.

